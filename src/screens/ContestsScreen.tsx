import * as React from 'react'
import { Box } from 'native-base'

import { HomeScreen } from '~/types/navigation'

export const ContestsScreen: HomeScreen<'Contests'> = ({}) => {
  return <Box></Box>
}
